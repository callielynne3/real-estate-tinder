import { connect } from 'react-redux';
import rentalQueries from '../components/rentalQueries';

const mapStateToProps = (state) => ({ });

export default connect(mapStateToProps, null)(rentalQueries);
