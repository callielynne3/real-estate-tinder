import { connect } from 'react-redux';
import WelcomeLinks from '../components/welcome_links/WelcomeLinks';

const mapStateToProps = (state) => ({ });

export default connect(mapStateToProps, null)(WelcomeLinks);
