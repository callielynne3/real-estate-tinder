import { connect } from 'react-redux';
import MapsSearchBox from '../components/MapsSearchBox';

const mapStateToProps = (state) => ({ });

export default connect(mapStateToProps, null)(MapsSearchBox);
